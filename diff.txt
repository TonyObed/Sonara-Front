diff --git a/index.html b/index.html
index 1d93a86..e592b04 100644
--- a/index.html
+++ b/index.html
@@ -583,44 +583,167 @@
       100% { height: 90%; }
     }
 
-    /* ÔöÇÔöÇÔöÇ STATS SECTION ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
+    /* ÔöÇÔöÇÔöÇ STATS SECTION (BENTO GRID) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
     #stats {
-      padding: 50px 0;
-      background: var(--bg-secondary);
+      padding: 80px 0;
+      background: var(--bg-primary);
       border-top: 1px solid var(--border);
       border-bottom: 1px solid var(--border);
+      position: relative;
     }
     .stats-grid {
       display: grid;
-      grid-template-columns: repeat(4, 1fr);
-      gap: 1px;
-      background: var(--border);
-      border-radius: var(--radius-lg);
-      overflow: hidden;
-      border: 1px solid var(--border);
+      grid-template-columns: repeat(3, 1fr);
+      gap: 20px;
+      background: transparent;
+      border-radius: 0;
+      overflow: visible;
+      border: none;
     }
     .stat-card {
-      background-color: var(--bg-secondary);
+      background-color: rgba(255, 255, 255, 0.02);
+      backdrop-filter: blur(8px);
+      border: 1px solid var(--border);
+      border-radius: var(--radius-lg);
       padding: 36px 28px;
       text-align: center;
-      transition: background-color 200ms;
+      display: flex;
+      flex-direction: column;
+      justify-content: center;
+      align-items: center;
+      gap: 8px;
+      transition: all 400ms var(--ease-snap);
+      opacity: 0;
+      transform: translateY(30px);
+    }
+    .stat-card.animate-in {
+      opacity: 1;
+      transform: translateY(0);
     }
     .stat-card:hover {
-      background-color: var(--bg-tertiary);
+      background-color: rgba(255, 255, 255, 0.04);
+      border-color: var(--brand-accent-h);
+      box-shadow: 0 10px 30px rgba(0, 82, 255, 0.05);
+      transform: translateY(-4px);
+    }
+    .stat-card.large {
+      grid-column: span 2;
+      text-align: left;
+      flex-direction: row;
+      justify-content: space-between;
+      align-items: center;
+    }
+    .stat-card.large .stat-content {
+      max-width: 70%;
+    }
+    .stat-card.large .stat-graphic {
+      display: flex;
+      align-items: center;
+      justify-content: center;
+      width: 72px;
+      height: 72px;
+      background: rgba(0, 82, 255, 0.05);
+      border: 1px solid rgba(0, 82, 255, 0.15);
+      border-radius: 50%;
+      flex-shrink: 0;
     }
     .stat-number {
       font-family: var(--font-display);
-      font-size: 42px;
-      font-weight: 800;
+      font-size: 48px;
+      font-weight: 850;
       color: var(--brand-accent);
       line-height: 1;
-      margin-bottom: 8px;
-      font-variation-settings: 'opsz' 42;
+      margin-bottom: 6px;
+      font-variation-settings: 'opsz' 48;
     }
     .stat-label {
-      font-size: 13px;
+      font-size: 14px;
       color: var(--text-secondary);
-      line-height: 20px;
+      line-height: 22px;
+    }
+    /* Wave equalizer visual */
+    .voice-wave {
+      display: flex;
+      align-items: flex-end;
+      gap: 3px;
+      height: 24px;
+    }
+    .voice-wave-bar {
+      width: 3px;
+      background: var(--brand-accent);
+      border-radius: var(--radius-full);
+      animation: pulse-wave-bar 1.2s infinite ease-in-out alternate;
+    }
+    .voice-wave-bar:nth-child(1) { height: 10px; animation-delay: 0.1s; }
+    .voice-wave-bar:nth-child(2) { height: 24px; animation-delay: 0.3s; }
+    .voice-wave-bar:nth-child(3) { height: 15px; animation-delay: 0.2s; }
+    .voice-wave-bar:nth-child(4) { height: 20px; animation-delay: 0.5s; }
+    .voice-wave-bar:nth-child(5) { height: 8px; animation-delay: 0.4s; }
+    @keyframes pulse-wave-bar {
+      0% { transform: scaleY(0.3); }
+      100% { transform: scaleY(1); }
+    }
+    /* Circular pulse activity visual */
+    .voice-pulse-ring {
+      position: relative;
+      width: 32px;
+      height: 32px;
+      display: flex;
+      align-items: center;
+      justify-content: center;
+    }
+    .voice-pulse-circle {
+      position: absolute;
+      width: 100%;
+      height: 100%;
+      border: 2px solid var(--brand-accent);
+      border-radius: 50%;
+      animation: pulse-circle-glow 1.8s infinite ease-out;
+    }
+    .voice-pulse-circle:nth-child(2) {
+      animation-delay: 0.6s;
+    }
+    .voice-pulse-circle:nth-child(3) {
+      animation-delay: 1.2s;
+    }
+    .voice-pulse-dot {
+      width: 8px;
+      height: 8px;
+      background: var(--brand-accent);
+      border-radius: 50%;
+    }
+    @keyframes pulse-circle-glow {
+      0% {
+        transform: scale(0.6);
+        opacity: 1;
+      }
+      100% {
+        transform: scale(1.6);
+        opacity: 0;
+      }
+    }
+    @media (max-width: 1024px) {
+      .stats-grid {
+        grid-template-columns: repeat(2, 1fr);
+      }
+      .stat-card.large {
+        grid-column: span 2;
+      }
+    }
+    @media (max-width: 640px) {
+      .stats-grid {
+        grid-template-columns: 1fr;
+        gap: 16px;
+      }
+      .stat-card.large {
+        grid-column: span 1;
+        flex-direction: column;
+        align-items: center;
+        text-align: center;
+      }
+      .stat-card.large .stat-content {
+        max-width: 100%;
+      }
     }
 
     /* ÔöÇÔöÇÔöÇ HOW IT WORKS ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
@@ -2039,10 +2162,17 @@
       background: radial-gradient(circle, rgba(0, 82, 255, 0.08) 0%, rgba(27, 79, 114, 0.04) 40%, transparent 70%);
     }
     body.light-mode .stat-card {
-      background-color: var(--bg-secondary);
+      background-color: rgba(0, 0, 0, 0.02);
+      border-color: rgba(0, 0, 0, 0.05);
     }
     body.light-mode .stat-card:hover {
-      background-color: var(--bg-tertiary);
+      background-color: rgba(0, 0, 0, 0.04);
+      border-color: var(--brand-accent);
+      box-shadow: 0 10px 30px rgba(0, 82, 255, 0.04);
+    }
+    body.light-mode .stat-card.large .stat-graphic {
+      background: rgba(0, 82, 255, 0.02);
+      border-color: rgba(0, 82, 255, 0.08);
     }
     body.light-mode .step-card {
       background: var(--bg-secondary);
@@ -2240,22 +2370,47 @@
   <!-- ÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉ STATS -->
   <section id="stats">
     <div class="container">
-      <div class="stats-grid reveal">
-        <div class="stat-card">
-          <div class="stat-number" data-target="75">0%</div>
-          <div class="stat-label">R├®duction des co├╗ts par rapport ├á un centre d'appels</div>
+      <div class="stats-grid">
+        <!-- Card 1 (Large - Spans 2 Cols) -->
+        <div class="stat-card large">
+          <div class="stat-content">
+            <div class="stat-number" data-target="75" data-suffix="%">0%</div>
+            <div class="stat-label">R├®duction moyenne des co├╗ts d'exploitation par rapport ├á un centre d'appels traditionnel</div>
+          </div>
+          <div class="stat-graphic">
+            <div class="voice-wave" aria-hidden="true">
+              <span class="voice-wave-bar"></span>
+              <span class="voice-wave-bar"></span>
+              <span class="voice-wave-bar"></span>
+              <span class="voice-wave-bar"></span>
+              <span class="voice-wave-bar"></span>
+            </div>
+          </div>
         </div>
+        <!-- Card 2 (Small - Spans 1 Col) -->
         <div class="stat-card">
-          <div class="stat-number" data-target="10">0 min</div>
-          <div class="stat-label">Temps moyen de g├®n├®ration des premiers rapports</div>
+          <div class="stat-number" data-target="100" data-suffix="k+">0k+</div>
+          <div class="stat-label">Capacit├® d'appels simultan├®s sans latence</div>
         </div>
+        <!-- Card 3 (Small - Spans 1 Col) -->
         <div class="stat-card">
-          <div class="stat-number" data-target="92">0%</div>
-          <div class="stat-label">Taux moyen de compl├®tion des enqu├¬tes par l'IA</div>
+          <div class="stat-number" data-target="10" data-prefix="&lt; " data-suffix=" min">0 min</div>
+          <div class="stat-label">G├®n├®ration de rapports d'analyse</div>
         </div>
-        <div class="stat-card">
-          <div class="stat-number" data-target="100">0k+</div>
-          <div class="stat-label">Capacit├® d'appels simultan├®s sans latence</div>
+        <!-- Card 4 (Large - Spans 2 Cols) -->
+        <div class="stat-card large">
+          <div class="stat-content">
+            <div class="stat-number" data-target="92" data-suffix="%">0%</div>
+            <div class="stat-label">Taux moyen de compl├®tion des enqu├¬tes men├®es int├®gralement par nos agents IA</div>
+          </div>
+          <div class="stat-graphic">
+            <div class="voice-pulse-ring" aria-hidden="true">
+              <span class="voice-pulse-circle"></span>
+              <span class="voice-pulse-circle"></span>
+              <span class="voice-pulse-circle"></span>
+              <span class="voice-pulse-dot"></span>
+            </div>
+          </div>
         </div>
       </div>
     </div>
@@ -3046,42 +3201,55 @@
 
     document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));
 
-    // ÔöÇÔöÇÔöÇ DYNAMIC STATISTICS COUNT UP ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+    // ÔöÇÔöÇÔöÇ DYNAMIC STATISTICS COUNT UP (BENTO & EASED) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
     const statsObserver = new IntersectionObserver((entries) => {
+      let delayOffset = 0;
       entries.forEach(entry => {
         if (entry.isIntersecting) {
-          const numberEl = entry.target;
+          const cardEl = entry.target;
+          const numberEl = cardEl.querySelector('.stat-number');
+          if (!numberEl) return;
+
           const target = parseInt(numberEl.getAttribute('data-target'), 10);
-          let current = 0;
-          const duration = 1500; // ms
-          const stepTime = Math.max(Math.floor(duration / target), 15);
-          
-          const timer = setInterval(() => {
-            current += 1;
-            if (numberEl.textContent.includes('%') || numberEl.textContent.includes('k+') || numberEl.textContent.includes(' min') || numberEl.textContent.includes(' FCFA')) {
-              // Extract original suffix
-              if (target === 75 || target === 92) numberEl.textContent = `${current}%`;
-              else if (target === 10) numberEl.textContent = `< ${current} min`;
-              else if (target === 100) numberEl.textContent = `${current}k+`;
-            } else {
-              if (target === 75 || target === 92) numberEl.textContent = `${current}%`;
-              else if (target === 10) numberEl.textContent = `< ${current} min`;
-              else if (target === 100) numberEl.textContent = `${current}k+`;
-              else numberEl.textContent = current;
-            }
+          const prefix = numberEl.getAttribute('data-prefix') || '';
+          const suffix = numberEl.getAttribute('data-suffix') || '';
+          const duration = 2000; // ms
+
+          // Easing function: Cubic Out
+          function easeOutCubic(t) {
+            return 1 - Math.pow(1 - t, 3);
+          }
+
+          // Staggered card slide-in and fade-in
+          setTimeout(() => {
+            cardEl.classList.add('animate-in');
             
-            if (current >= target) {
-              clearInterval(timer);
-              if (target === 75 || target === 92) numberEl.textContent = `${target}%`;
-              else if (target === 10) numberEl.textContent = `< ${target} min`;
-              else if (target === 100) numberEl.textContent = `${target}k+`;
+            // Start count-up animation
+            let startTime = null;
+            function animateCount(timestamp) {
+              if (!startTime) startTime = timestamp;
+              const progress = Math.min((timestamp - startTime) / duration, 1);
+              const easedProgress = easeOutCubic(progress);
+              const currentVal = Math.floor(easedProgress * target);
+              
+              numberEl.textContent = `${prefix}${currentVal}${suffix}`;
+              
+              if (progress < 1) {
+                requestAnimationFrame(animateCount);
+              } else {
+                numberEl.textContent = `${prefix}${target}${suffix}`;
+              }
             }
-          }, stepTime);
-          statsObserver.unobserve(numberEl);
+            requestAnimationFrame(animateCount);
+          }, delayOffset);
+
+          delayOffset += 150; // 150ms stagger delay between cards!
+          statsObserver.unobserve(cardEl);
         }
       });
-    }, { threshold: 0.5 });
-    document.querySelectorAll('.stat-number').forEach(el => statsObserver.observe(el));
+    }, { threshold: 0.15 });
+
+    document.querySelectorAll('.stat-card').forEach(el => statsObserver.observe(el));
 
     // ÔöÇÔöÇÔöÇ INTERACTIVE TAB SWITCHING (DASHBOARD SHOWCASE) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
     function switchTab(event, tabId) {
