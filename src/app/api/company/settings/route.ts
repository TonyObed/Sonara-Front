import { NextRequest } from "next/server";
import { db } from "@/lib/db";
import { authenticateRequest } from "@/lib/auth";
import { ok, unauthorized, forbidden, handleError } from "@/lib/response";
export async function GET(request: NextRequest) { try { const auth = await authenticateRequest(request); if (!auth) return unauthorized(); return ok(await db.companySetting.upsert({ where: { companyId: auth.companyId }, create: { companyId: auth.companyId }, update: {} })); } catch (error) { return handleError(error); } }
export async function PATCH(request: NextRequest) { try { const auth = await authenticateRequest(request); if (!auth) return unauthorized(); if (!["ADMIN", "MANAGER"].includes(auth.role)) return forbidden(); const body = await request.json(); const data = { timezone: typeof body.timezone === "string" ? body.timezone : undefined, displayPhone: typeof body.displayPhone === "string" ? body.displayPhone : undefined, transferAgentNumber: typeof body.transferAgentNumber === "string" ? body.transferAgentNumber : undefined, webhookUrl: typeof body.webhookUrl === "string" ? body.webhookUrl : undefined, maxConcurrentCalls: Number.isInteger(body.maxConcurrentCalls) && body.maxConcurrentCalls > 0 && body.maxConcurrentCalls <= 100 ? body.maxConcurrentCalls : undefined, autoRechargeEnabled: typeof body.autoRechargeEnabled === "boolean" ? body.autoRechargeEnabled : undefined, autoRechargeThreshold: Number.isInteger(body.autoRechargeThreshold) && body.autoRechargeThreshold >= 0 ? body.autoRechargeThreshold : undefined }; return ok(await db.companySetting.upsert({ where: { companyId: auth.companyId }, create: { companyId: auth.companyId, ...data }, update: data })); } catch (error) { return handleError(error); } }
